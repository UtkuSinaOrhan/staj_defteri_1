# Staj Günlüğü - 16 Temmuz 2026

- **İleri Seviye Komutlar ve Dağıtım:** 
  - `--build-arg` keywordünün kullanımı öğrenildi.
  - `docker commit` komutu ile çalışan container üzerinden imaj üretme çalışıldı.
  - `docker save` ve `docker load` komutları ile imajların yedeklenmesi ve taşınması sağlandı.
  - Docker registry kullanılarak local (yerel) bir Docker image reposu oluşturuldu.
- **Alıştırmalar:** Docker image ve registry bölüm sonu alıştırmaları başarıyla tamamlandı.